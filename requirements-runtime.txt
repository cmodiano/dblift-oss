# DBLift Runtime Dependencies (Minimal for Docker images)
# Use this for production Docker images instead of requirements.txt
# IMPORTANT: Keep versions in sync with pyproject.toml [project].dependencies

# Core dependencies
PyYAML>=6.0.1
tabulate>=0.9.0
python-dateutil>=2.8.2
typing-extensions>=4.7.1

# CLI and formatting
click>=8.1.7
rich>=13.3.0
Jinja2>=3.1.0

# SQL validation (can be optional for migration-only images)
# NOTE: Version must match pyproject.toml (>=30.0.0)
sqlglot>=30.0.0
sqlfluff>=4.2.2

# Azure Cosmos DB support
azure-cosmos>=4.5.0
azure-identity>=1.25.3

# Secrets manager providers (Vault, AWS, Azure Key Vault, GCP)
hvac>=2.4.0
boto3>=1.43.29
azure-keyvault-secrets>=4.11.0
google-cloud-secret-manager>=2.29.0

# Native driver integration
SQLAlchemy>=2.0.0

# Licensing/security
PyJWT>=2.12.0
cryptography>=49.0.0
