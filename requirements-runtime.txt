# DBLift Runtime Dependencies (Minimal for Docker images)
# Use this for production Docker images instead of requirements.txt
# IMPORTANT: Keep versions in sync with pyproject.toml [project].dependencies

# Core dependencies
PyYAML>=6.0.1
tabulate>=0.9.0
python-dateutil>=2.8.2
typing-extensions>=4.7.1

# CLI and formatting
click>=8.1.7
rich>=13.3.0
Jinja2>=3.1.0

# SQL validation (can be optional for migration-only images)
# NOTE: Version must match pyproject.toml (>=30.0.0)
sqlglot>=30.0.0
sqlfluff>=4.0.0

# Native driver integration
SQLAlchemy>=2.0.0

# Optional database drivers — uncomment the ones you need,
# or use pip extras: pip install "dblift[postgresql,mysql,oracle,...]"
# See [project.optional-dependencies] in pyproject.toml for the full list.
#
# psycopg[binary]>=3.1   # PostgreSQL  (dblift[postgresql])
# PyMySQL>=1.1           # MySQL/MariaDB (dblift[mysql])
# oracledb>=2.0          # Oracle       (dblift[oracle])
# pymssql>=2.2           # SQL Server   (dblift[sqlserver])
# ibm_db>=3.2            # DB2          (dblift[db2])
# ibm_db_sa>=0.4         # DB2 SQLAlchemy dialect (dblift[db2])
# azure-cosmos>=4.5      # CosmosDB     (dblift[cosmosdb])
